// DCMotor.h
/*
#ifndef _DCMOTOR_h
#define _DCMOTOR_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

class DCMotor
{
 protected:
	 const int A1Pin;
	 const int A2Pin;
	 const int ASPin;

	 const int B1Pin;
	 const int B2Pin;
	 const int BSPin;

	 int speed;


 public:
	 DCMotor(int A1, int A2, int AS, int B1, int B2, int BS);

	 void front();
	 void back();
	 void turnLeft();
	 void turnRight();
};

#endif*/

